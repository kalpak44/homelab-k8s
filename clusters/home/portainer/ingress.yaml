apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: portainer
  namespace: home
spec:
  entryPoints:
    - private
  routes:
    - match: Host(`portainer.internal`)
      kind: Rule
      services:
        - name: portainer
          port: 9000
